apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring-stack
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '10'
spec:
  project: aquarela-platform
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  source:
    repoURL: https://prometheus-community.github.io/helm-charts
    chart: kube-prometheus-stack
    targetRevision: 80.13.3
    helm:
      releaseName: kube-prometheus-stack
      values: |
        grafana:
          adminPassword: change-me-now
          service:
            type: ClusterIP
        alertmanager:
          config:
            global:
              smtp_smarthost: smtp.gmail.com:587
              smtp_from: guixerosk62@gmail.com
              smtp_auth_username: guixerosk62@gmail.com
              smtp_auth_password_file: /etc/alertmanager/secrets/aquarela-alertmanager-smtp/smtp_auth_password
              smtp_require_tls: true
            route:
              receiver: team-mail
              group_by: ['alertname']
              group_wait: 30s
              group_interval: 5m
              repeat_interval: 4h
            receivers:
              - name: team-mail
                email_configs:
                  - to: guilhermeferreirskt@gmail.com
                    send_resolved: true
              - name: 'null'
            routes:
              - receiver: 'null'
                matchers:
                  - alertname = "Watchdog"
          alertmanagerSpec:
            secrets:
              - aquarela-alertmanager-smtp
        prometheus:
          prometheusSpec:
            serviceMonitorSelector: {}
            serviceMonitorSelectorNilUsesHelmValues: false
            serviceMonitorNamespaceSelector: {}
            podMonitorSelector: {}
            podMonitorSelectorNilUsesHelmValues: false
            podMonitorNamespaceSelector: {}
            ruleSelector: {}
            ruleSelectorNilUsesHelmValues: false
            ruleNamespaceSelector: {}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - Replace=true
      - SkipDryRunOnMissingResource=true

